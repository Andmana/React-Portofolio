import Home from "../Home";

const Main = () => {
    return (
        <main className="py-14 flex flex-col gap-14 isolate">
            <Home />
            <section className="h-screen" />
            <section className="h-screen" />
        </main>
    );
};

export default Main;
